<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <item name="android:textViewStyle">@style/textViewDefault</item>
    </style>

    <style name="textViewDefault" parent="android:Widget.TextView">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="textViewBoldSmall" parent="textViewDefault">
        <item name="android:textStyle">bold</item>
    </style>

    <style name="textViewBoldMedium" parent="textViewDefault">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">16dp</item>
    </style>

    <style name="textViewLarge" parent="textViewDefault">
        <item name="android:textSize">20dp</item>
    </style>

</resources>
